// You can specify database schema here (I haven't define any in this version)

// OR

// You can also add CRUD functions here and then call it in crmController (which is the general workflow in large scale application)